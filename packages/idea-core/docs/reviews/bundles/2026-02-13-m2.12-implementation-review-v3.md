# M2.12 Implementation Review Bundle (v3)

Date: 2026-02-13  
Repo: `/Users/fkg/Nutstore Files/Coding/Agents/idea-core`  
Milestone: `M2.12`

## 1) Goal / Scope / Out-of-scope

Goal:
- Deliver fixed-seed replayable demo campaign:
  - `campaign.init -> search.step -> eval.run -> rank.compute -> node.promote`
- Produce auditable artifacts + manifest.
- Add structural isomorphism comparison (with negative test).
- Preserve idempotency semantics and budget/accounting behavior.

In scope:
- `src/idea_core/demo/m2_12_replay.py`
- `src/idea_core/demo/schemas/demo_manifest_v1.schema.json`
- `scripts/run_m2_12_demo.sh`
- `tests/engine/test_m2_12_demo_replay.py`
- Blocker fix: `src/idea_core/engine/store.py` (`file://` URI decode).
- Demo evidence directory: `docs/demos/2026-02-13-m2.12-demo-v1/`

Out-of-scope:
- Any OpenRPC/schema snapshot change under `contracts/idea-generator-snapshot/`.
- M3/M4/M5 features.

## 2) Diff Evidence

Commits:
- `56c67f9` feat(demo): add M2.12 fixed-seed replay campaign runner
- `109566c` test(demo): add M2.12 replay smoke and isomorphism coverage
- `8e47063` fix(store): decode file URI paths for artifact refs
- `cf20449` feat(demo): validate M2.12 manifest schema and add compare negative test

`git show --stat` (summary):
- Replay runner + CLI script + demo package scaffold.
- New test module for smoke/isomorphic/idempotency/negative-compare.
- URI decode fix in store for `file://...%20...` paths.
- New demo manifest schema + runtime validation.

## 3) Commands + Outputs

### 3.1 Regression

```bash
make validate && pytest
```

Output:

```text
python3 -m idea_core.contracts.validate
[validate] OK: JSON parse, schema legality, OpenRPC minimal shape, $ref closure, drift-guard
.....................................                                    [100%]
37 passed in 0.93s
```

### 3.2 Demo run + isomorphism check

```bash
./scripts/run_m2_12_demo.sh --output-dir "docs/demos/2026-02-13-m2.12-demo-v1" --run-tag run-001
./scripts/run_m2_12_demo.sh --output-dir "docs/demos/2026-02-13-m2.12-demo-v1" --run-tag run-002 --compare-with "docs/demos/2026-02-13-m2.12-demo-v1/run-001/demo_manifest.json"
```

Output:

```text
manifest: file:///Users/fkg/Nutstore%20Files/Coding/Agents/idea-core/docs/demos/2026-02-13-m2.12-demo-v1/run-001/demo_manifest.json
manifest: file:///Users/fkg/Nutstore%20Files/Coding/Agents/idea-core/docs/demos/2026-02-13-m2.12-demo-v1/run-002/demo_manifest.json
isomorphism_report: file:///Users/fkg/Nutstore%20Files/Coding/Agents/idea-core/docs/demos/2026-02-13-m2.12-demo-v1/run-002/isomorphism_report.json
```

Verification snapshots:
- `run-001/demo_manifest.json`: `validations.status = pass`
- `run-002/isomorphism_report.json`: `isomorphic = true`, `first_diff_path = null`

## 4) Contract Alignment Checklist

- [x] No contract drift: OpenRPC/schema snapshot untouched.
- [x] Uses only existing RPC methods and required flow.
- [x] Demo manifest includes:
  - campaign_id
  - input summaries + idempotency keys
  - key outputs (`new_node_ids`, artifact refs, ranked nodes, handoff ref)
  - validation summary (replay/idempotency/artifact existence/isomorphism)
- [x] Isomorphism logic implemented and tested with:
  - positive case (`isomorphic=true`)
  - negative case (`isomorphic=false`, path pinpoint)
- [x] Idempotency replay validated in runner and tests.
- [x] Full regression green.

## 5) Key Code Snippets (Verbatim)

### 5.1 URI decode fix (`src/idea_core/engine/store.py`)

```python
parsed = urlparse(artifact_ref)
if parsed.scheme != "file":
    raise FileNotFoundError(f"unsupported artifact ref: {artifact_ref}")
path = Path(unquote(parsed.path)).resolve()
```

### 5.2 Manifest schema validation (`src/idea_core/demo/m2_12_replay.py`)

```python
schema = json.loads(DEMO_SCHEMA_PATH.read_text(encoding="utf-8"))
validator = Draft202012Validator(schema, format_checker=FormatChecker())
errors = sorted(validator.iter_errors(manifest), key=lambda err: list(err.path))
```

### 5.3 Negative structural diff test (`tests/engine/test_m2_12_demo_replay.py`)

```python
manifest_b["key_results"]["rank_compute"]["method"] = "elo"
report = compare_manifests(manifest_a, manifest_b)
assert report["isomorphic"] is False
assert report["first_diff_path"] == "$.key_results.rank_compute.method"
```

