# M2.7 Implementation Review Bundle (v2)

Date: 2026-02-13  
Repo: `/Users/fkg/Nutstore Files/Coding/Agents/idea-core`  
Milestone: `M2.7` (Explain-Then-Formalize 强制: `RationaleDraft -> IdeaCard`)

## 1) 目标 / 范围 / 排除项

目标:
- 在节点生成路径落地最小可审计的 `RationaleDraft -> IdeaCard` 形式化链路。
- 对无 `IdeaCard` 节点维持 `node.promote` 强制失败（schema gate 语义 + 既有错误模型）。
- 不回归 `search/eval/rank` 联动与 idempotency replay/conflict 语义。

范围:
- `src/idea_core/engine/service.py`
- `tests/engine/test_node_promote.py`
- `tests/engine/test_search_step_operator_m26.py`

排除项:
- 不改 `contracts/idea-generator-snapshot/*`（OpenRPC/schema SSOT 保持不变）。
- 不推进 M2.8+/M3+ 功能。
- 不改 hepar/runtime-adapter 相关代码。

## 2) Diff 证据

实现提交:
- `7600e67` feat(engine): enforce explain-then-formalize path for M2.7
- `4788636` fix(promote): validate formalization trace provenance for M2.7

关键行为变化:
- 新增 `_formalize_rationale_to_idea_card(...)`，统一从 `rationale_draft` 生成 schema-valid `idea_card`。
- seed 节点与 operator 节点统一走上述路径，并在 `operator_trace.params.formalization` 写入审计字段。
- formalization mode 明确为 deterministic：`explain_then_formalize_deterministic_v1`。
- `node.promote` 新增 formalization provenance gate：
  - 必须存在 `operator_trace.params.formalization`；
  - `source_artifact` 必须是 `rationale_draft`；
  - `rationale_hash` 必须与当前 `rationale_draft` 一致；
  - 失败返回既有错误模型：`-32002/schema_validation_failed`。

## 3) 验收命令与结果

```bash
make validate && pytest
```

Output:

```text
python3 -m idea_core.contracts.validate
[validate] OK: JSON parse, schema legality, OpenRPC minimal shape, $ref closure, drift-guard
...........................................                              [100%]
43 passed in 0.83s
```

## 4) M2.7 条款映射

- [x] 有 `IdeaCard` 时 `node.promote` 可通过（`test_node_promote_success_without_reduction`）。
- [x] 无 `IdeaCard` 时 `node.promote` 必失败（`test_node_promote_fails_when_idea_card_missing`）。
- [x] promote idempotency replay 不重复副作用（`test_node_promote_idempotency_replay_does_not_duplicate_side_effects`）。
- [x] 同 key 不同 payload 冲突包含 `payload_hash`（`test_node_promote_idempotency_conflict_includes_payload_hash`）。
- [x] search/eval/rank 与形式化字段协同不回归（`test_search_step_nodes_flow_into_eval_rank_promote_smoke` + `test_generated_nodes_visible_via_node_list_cursor_and_node_get`）。
- [x] 形式化路径可审计且可校验（`test_node_promote_fails_when_formalization_trace_missing` + `test_node_promote_fails_when_formalization_hash_mismatch`）。

## 5) Reviewer 关注点

请重点检查:
1. 形式化路径是否是最小闭环且可审计（deterministic but explicit）。
2. `node.promote` gate 是否覆盖无卡/trace 缺失/hash 不一致。
3. replay/conflict、预算、分页语义是否保持无回归。
4. 变更是否严格限制在 M2.7（无 M2.8+/M3 漂移）。
