You are a senior research software architect reviewing an M3.5 implementation (v1).

STRICT OUTPUT CONTRACT (must follow exactly):
1) First line must be exactly one of:
   - VERDICT: READY
   - VERDICT: NOT_READY
2) Must contain exact headers:
   - ## Blockers
   - ## Non-blocking
   - ## Real-research fit
   - ## Robustness & safety
   - ## Specific patch suggestions

Task:
Review whether M3.5 is complete and acceptable.

Scope constraints:
- Only M3.5 (HEP constraints/validators minimum set).
- No M3.6+/M4+ expansion.
- No OpenRPC/schema semantic changes.

Implemented artifacts:
- src/idea_core/engine/service.py
- tests/engine/test_hep_constraints_m35.py
- Review bundle: docs/reviews/bundles/2026-02-13-m3.5-implementation-review-v1.md

Acceptance target:
- At least two heuristic classes are implemented:
  - consistency checks (dimension / known constraints)
  - feasibility checks (compute-plan realism)
- `eval.run` outputs structured `failure_modes` in deterministic token format.
- `eval.run` maps validator findings to structured `fix_suggestions`.
- `node.promote` blocks on critical HEP constraint failures with structured diagnostics.
- Regression tests cover eval emission and promote gate path.

Verification evidence:
- red->green failure/fix log: docs/reviews/bundles/2026-02-13-m3.5-hep-constraints-failure-repro-v1.txt
- gate: docs/reviews/bundles/2026-02-13-m3.5-validate-and-pytest-v1.txt

Please decide READY/NOT_READY with concrete blockers only if they prevent accepting M3.5.

