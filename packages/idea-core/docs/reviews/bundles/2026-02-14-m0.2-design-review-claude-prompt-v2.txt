You are a senior research software architect reviewing an M0.2 Design packet (v1) for an evidence-first physics computation test project that also serves as an R&D testbed for the idea-generator / idea-core research automation ecosystem (program milestone M5).

STRICT OUTPUT CONTRACT (must follow exactly; HARD REQUIREMENT):
1) The VERY FIRST LINE of your response must be exactly one of:
   - VERDICT: READY
   - VERDICT: NOT_READY
   Do NOT write any preamble, throat-clearing, or meta commentary before the verdict line.
2) After the first line, your response MUST contain these exact headers:
   - ## Blockers
   - ## Non-blocking
   - ## Real-research fit
   - ## Robustness & safety
   - ## Specific patch suggestions

Decision policy (critical):
- Judge ONLY the M0.2 acceptance criteria below.
- Mark NOT_READY only if one or more acceptance criteria are not satisfied, or evidence is missing in THIS packet.
- Do not require external filesystem access; treat the packet below as authoritative evidence.

Scope constraints:
- Only M0.2 (design + I/O + gates).
- Out of scope: any implementation, solver installation, numerical results, or M0.3+ prototype work.

Acceptance criteria for M0.2 (must evaluate exactly these):
1) Layer split is explicit and enforced: idea-core is control-plane only; compute is Julia.
2) Compute-plane I/O contract is specified and stable: config.json -> results.json keys.
3) Physics pipeline includes a concrete “modern bootstrap” insertion point and explicitly avoids pure dispersive reproduction as the main method.
4) Evidence-first gate requirements are specified for M0.2 (dual review, artifact paths) and framed for automation (machine-checkable intent).
5) “Don’t forget” requirement is internalized: design makes workflow rules hard to forget by encoding them as gate artifacts (not only prose), with a clear plan to enforce in M0.3.

Authoritative packet (summary; treat as SSOT for this review):

- Context: “Pion GFF Bootstrap” is a real test project for the research automation ecosystem (idea-generator / idea-core). Primary product is workflow + evidence + gates, not only physics results.
- Chosen option: A (Julia compute-plane; idea-core control-plane).
- Hard constraints:
  1) no coupled-channel; elastic-only, treat inelastic thresholds as truncation envelopes
  2) pion-only
  3) laptop-only ≤ 3 days
  4) evidence-first: all key claims traceable to code outputs + paper evidence
  5) modern bootstrap; no pure dispersive reproduction as main method
- Repo layout proposal:
  - control-plane docs: docs/research/pion-gff-bootstrap/{tracker.md,m0.1-preflight.md,m0.2-design.md,runs/,sync/}
  - compute-plane Julia project: research/pion_gff_bootstrap/{Project.toml,Manifest.toml,src/,bin/run.jl,configs/,out/}
- Compute I/O contract (v1):
  - Input: config.json with run_id, physics params, bootstrap method + constraints, objective targets, numerics threads/tolerances
  - Output: results.json with bounds on Dpi_0 and r_g2 plus stability + provenance
- Physics minimal pipeline:
  - Frozen observables: D^pi(0) and slope/radius from A^pi'(0) (basis mapped later)
  - Evidence anchors: 2411.13398 pion EMT decomposition + elastic unitarity relations (Eq. pi_space, ImApi, ImDpi) with local TeX file path recorded
  - Bootstrap insertion point: generate allowed family of elastic partial waves (t_0^0, t_2^0) using modern bootstrap constraints inspired by 2410.23333 / 2403.10772 / 2505.19332; propagate to GFF bounds by envelopes
  - Truncation handling for t > elastic_t_max: conservative envelope family; report dependence
- Gate definition:
  - M0.2 gate requires: design doc artifact, review bundle, dual-review outputs (Opus + gemini-3-pro-preview) with meta + trace artifacts
  - Acceptance criteria for M0.2 enumerated in the design doc
- “Borrow from brainstorming” stance:
  - adopt design-gate + explicit acceptance + evidence, but implement as ecosystem primitives (MilestoneGate artifacts + validators + wrappers)

Please decide READY/NOT_READY strictly against the M0.2 criteria above, and provide minimal, concrete patch suggestions (file path + what to change).

