You are a senior research software architect reviewing an M0.2 Design packet (v1) for an evidence-first physics computation test project that also serves as an R&D testbed for the idea-generator / idea-core research automation ecosystem (program milestone M5).

STRICT OUTPUT CONTRACT (must follow exactly):
1) First line must be exactly one of:
   - VERDICT: READY
   - VERDICT: NOT_READY
2) Must contain exact headers:
   - ## Blockers
   - ## Non-blocking
   - ## Real-research fit
   - ## Robustness & safety
   - ## Specific patch suggestions

Task:
Review whether M0.2 is complete and acceptable.

Scope constraints:
- Only M0.2 (design + I/O + gates).
- Treat the packet summary below as authoritative evidence (no external browsing required).
- Out of scope: implementation details, solver installation, numerical results (M0.3+).

Acceptance target (M0.2):
- Layered architecture is explicit (idea-core control-plane only; Julia compute-plane).
- Compute-plane I/O contract (config.json -> results.json) is concrete.
- Physics pipeline includes a modern bootstrap insertion point; not pure dispersive reproduction.
- Evidence-first gates are specified for M0.2 (dual review + artifact paths) with intent for automation.
- “Don’t forget” requirement is internalized (machine-checkable gates planned).

Authoritative packet (summary):
- Context: “Pion GFF Bootstrap” is a real test project for the idea-generator / idea-core research automation ecosystem (M5). Primary product is workflow + evidence + gates, not only physics results.
- Chosen option: A (Julia compute-plane; idea-core control-plane).
- Hard constraints: no coupled-channel; pion-only; laptop-only ≤ 3 days; evidence-first; modern bootstrap (no pure dispersive reproduction).
- Repo layout proposal:
  - docs/research/pion-gff-bootstrap/{tracker.md,m0.1-preflight.md,m0.2-design.md,runs/,sync/}
  - research/pion_gff_bootstrap/{Project.toml,Manifest.toml,src/,bin/run.jl,configs/,out/}
- I/O contract:
  - Input: config.json (run_id, physics params, bootstrap config, objective, numerics)
  - Output: results.json (bounds: Dpi_0 and r_g2; stability metadata; provenance)
- Physics pipeline:
  - Evidence anchor: 2411.13398 pion EMT definition + elastic unitarity equations (pi_space, ImApi, ImDpi) with local TeX path recorded.
  - Bootstrap insertion: generate allowed family of elastic partial waves (t_0^0, t_2^0) using modern bootstrap constraints inspired by 2410.23333 / 2403.10772 / 2505.19332; propagate to GFF and bound by envelopes.
  - Truncation for t > elastic_t_max via conservative envelopes; report dependence.
- M0.2 gate: requires design doc, review bundle, dual review outputs (opus + gemini-3-pro-preview) with meta.json + trace.jsonl.
- “Borrow from brainstorming”: adopt design-gate discipline but implement as ecosystem primitives (MilestoneGate artifacts + validators + wrappers).

Please decide READY/NOT_READY with concrete blockers only if they prevent accepting M0.2.

