# Review Bundle: 2026-02-13 M3.5 HEP constraints/validators minimum set (v1)

## 1. Scope in this review

- Milestone: **M3.5 only**
- Goal: add minimal HEP consistency/feasibility validators, emit structured `failure_modes`, and wire regression through `eval.run -> node.promote`.
- Explicitly out of scope: M3.6+/M4+, OpenRPC/schema semantic edits.

## 2. Files under review

- `src/idea_core/engine/service.py`
- `tests/engine/test_hep_constraints_m35.py`
- `docs/reviews/bundles/2026-02-13-m3.5-hep-constraints-failure-repro-v1.txt`
- `docs/reviews/bundles/2026-02-13-m3.5-validate-and-pytest-v1.txt`

## 3. Acceptance mapping (M3.5)

1. 至少 2 类一致性/可行性启发式
   - `consistency`:
     - `hep.dimension_units.v1` (mass/energy/lifetime unit checks)
     - `hep.known_constraints.v1` (branching ratio bounds, massless-vs-positive-mass contradiction)
   - `feasibility`:
     - `hep.compute_feasibility.v1` (placeholder compute methods, infra/compute mismatch, heavy-claim-without-cluster plan)

2. 结构化 `failure_modes`
   - `eval.run` writes tokenized diagnostics:
     - format: `hep:{class}:{validator_id}:{code}:{severity}`
     - examples:
       - `hep:consistency:hep.known_constraints.v1:branching_ratio_out_of_range:critical`
       - `hep:feasibility:hep.compute_feasibility.v1:compute_plan_placeholder_method:critical`

3. 与 eval/promote 路径可回归
   - `eval.run` appends HEP diagnostics into scorecards + `node.eval_info`.
   - `node.promote` blocks when `eval_info.failure_modes` contains `hep:*:*:*:critical`.
   - Regression tests assert:
     - structured emission + suggestion mapping (`physics_inconsistency`/`not_computable`)
     - promote hard-gate failure with structured `blocking_failure_modes`.

## 4. TDD red-green evidence

- `docs/reviews/bundles/2026-02-13-m3.5-hep-constraints-failure-repro-v1.txt`
  - RED: new tests fail before implementation (no HEP failure modes, no promote gate).
  - GREEN: targeted suite passes after implementation.

## 5. Full gate evidence

- `docs/reviews/bundles/2026-02-13-m3.5-validate-and-pytest-v1.txt`
  - `make validate` OK
  - `pytest` all green (`65 passed`).

## 6. Source excerpts (clean-room)

### `src/idea_core/engine/service.py` (validators + structured tokens)
```python
    def _build_hep_constraint_findings(...):
        ...
        add_finding(
            heuristic_class="consistency",
            validator_id="hep.known_constraints.v1",
            code="branching_ratio_out_of_range",
            severity="critical",
            failure_mode="physics_inconsistency",
            ...
        )
        ...
        add_finding(
            heuristic_class="feasibility",
            validator_id="hep.compute_feasibility.v1",
            code="compute_plan_placeholder_method",
            severity="critical",
            failure_mode="not_computable",
            ...
        )

    def _hep_failure_mode_token(...):
        return f"hep:{heuristic_class}:{validator_id}:{code}:{severity}"
```

### `src/idea_core/engine/service.py` (`eval.run` integration)
```python
                self._append_hep_constraint_diagnostics(
                    node=node,
                    failure_modes=failure_modes,
                    fix_suggestions=fix_suggestions,
                )
                failure_modes = self._dedupe_preserve_order(failure_modes)
                fix_suggestions = self._dedupe_fix_suggestions(fix_suggestions)
```

### `src/idea_core/engine/service.py` (`node.promote` hard gate)
```python
            blocking_hep_modes = self._blocking_hep_failure_modes(node)
            if blocking_hep_modes:
                data = {
                    "reason": "schema_invalid",
                    "campaign_id": campaign_id,
                    "node_id": node_id,
                    "details": {
                        "message": "hep_constraints_failed",
                        "blocking_failure_modes": blocking_hep_modes,
                    },
                }
                error = RpcError(code=-32002, message="schema_validation_failed", data=data)
                ...
                raise error
```

### `tests/engine/test_hep_constraints_m35.py` (regression path)
```python
def test_eval_run_emits_structured_hep_failure_modes_and_fix_suggestions(...):
    ...
    classes = {mode.split(":")[1] for mode in hep_failure_modes}
    assert "consistency" in classes
    assert "feasibility" in classes
    assert any(mode.endswith(":critical") for mode in hep_failure_modes)

def test_node_promote_fails_on_blocking_hep_constraint_failure_modes(...):
    ...
    assert exc.data["details"]["message"] == "hep_constraints_failed"
    blocking_modes = exc.data["details"]["blocking_failure_modes"]
    assert all(mode.endswith(":critical") for mode in blocking_modes)
```

## 7. Reviewer checklist

- [ ] M3.5 acceptance met: >=2 heuristic classes (`consistency` + `feasibility`).
- [ ] `failure_modes` are structured and deterministic.
- [ ] `eval.run -> node.promote` gate is regression-covered.
- [ ] No schema/OpenRPC semantic drift.
- [ ] No M3.6+/M4+ scope creep.

