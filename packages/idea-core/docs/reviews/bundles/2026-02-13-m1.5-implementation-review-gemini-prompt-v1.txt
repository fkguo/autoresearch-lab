You are a senior research software architect reviewing an M1.5 implementation.

STRICT OUTPUT CONTRACT (must follow exactly):
1) First line exactly one of:
- VERDICT: READY
- VERDICT: NOT_READY
2) Must contain these exact headers:
- ## Blockers
- ## Non-blocking
- ## Real-research fit
- ## Robustness & safety
- ## Specific patch suggestions

Review for:
- M1.5 only (bundled OpenRPC tooling artifact)
- No expansion to M2.8+/M3+
- No schema/OpenRPC SSOT semantic drift

Now review the following bundle:

# M1.5 Implementation Review Bundle (v1)

Date: 2026-02-13  
Repo: `/Users/fkg/Nutstore Files/Coding/Agents/idea-core`  
Milestone: `M1.5`（可选 bundled OpenRPC 产物）

## 1) 目标 / 范围 / 排除项

目标:
- 生成 bundled OpenRPC 产物：`contracts/idea-generator-snapshot/schemas/idea_core_rpc_v1.bundled.json`。
- 将 bundle 一致性检查接入 `make validate` 与 CI。
- 提供可回归的失败示例（drift fail）与修复记录。

范围:
- `src/idea_core/contracts/bundle.py`
- `src/idea_core/contracts/validate.py`
- `tests/contracts/test_bundle_contracts.py`
- `scripts/sync_contracts_snapshot.sh`
- `Makefile`
- `.github/workflows/ci.yml`
- `contracts/idea-generator-snapshot/schemas/idea_core_rpc_v1.bundled.json`
- `contracts/idea-generator-snapshot/CONTRACT_SOURCE.json`
- `README.md`

排除项:
- 不修改 `idea_core_rpc_v1.openrpc.json` 与 `*.schema.json` 的语义。
- 不推进 M2.8+/M3+。
- 不改 hepar/runtime-adapter。

## 2) 关键实现变化

1. 新增 bundle 生成器
- 新文件：`src/idea_core/contracts/bundle.py`
- 能从 vendored OpenRPC + schema 生成单文件 bundled 产物（重写 `$ref` 到 `#/components/schemas/*`）
- 明确 bundle 仅为 tooling 兼容：`x-bundle-note = Generated artifact ... Do not hand-edit`

2. validate 新增 bundle consistency gate
- `run_validation(..., check_bundle=True)` 默认开启
- 新增校验：
  - bundled 文件存在且 JSON 可解析
  - bundled 文件与实时生成结果完全一致（drift fail-fast）
  - bundled OpenRPC 结构最小形状 + `$ref` closure 可通过

3. 工作流接入
- `Makefile` 新增 `bundle-contracts`
- `scripts/sync_contracts_snapshot.sh` 在同步后自动生成 bundled 产物
- 新增 GitHub Actions：`.github/workflows/ci.yml`，执行 `make validate` + `pytest`

4. 回归测试新增
- `test_bundle_can_be_generated_and_validated`
- `test_validate_fails_on_bundle_drift`
- `test_validate_recovers_after_rebundle`

## 3) 验收命令与结果

命令与完整输出见：
- `docs/reviews/bundles/2026-02-13-m1.5-validate-and-pytest-v1.txt`

核心结果:
- `make validate` 通过（包含 bundle-consistency）
- `pytest` 通过：`46 passed`

## 4) 失败示例与修复记录（可回归）

复现日志：
- `docs/reviews/bundles/2026-02-13-m1.5-bundle-drift-repro-v1.txt`

流程:
1. 生成 bundle
2. 人为篡改 bundled 文件（`info.title`）
3. `validate` 失败（`bundle artifact drift detected`）
4. 重新执行 bundle 生成
5. `validate` 恢复通过

该场景也由测试覆盖：
- `test_validate_fails_on_bundle_drift`
- `test_validate_recovers_after_rebundle`

## 5) M1.5 条款映射

- [x] 生成 bundled OpenRPC 产物（非 SSOT、不可手改）
- [x] 接入 `make validate` bundle 一致性检查
- [x] 接入 CI 的 bundle 一致性检查（通过 `make validate`）
- [x] 至少 1 个失败示例与修复记录
- [x] `make validate && pytest` 全绿

## 6) Reviewer 关注点

请重点检查：
1. bundle 是否严格是 tooling artifact，而非新的契约 SSOT。
2. drift 检查是否 fail-fast 且错误信息可操作。
3. 失败示例与修复路径是否可复现实操。
4. 变更是否严格停留在 M1.5（无 M2.8+/M3+ 漂移）。
