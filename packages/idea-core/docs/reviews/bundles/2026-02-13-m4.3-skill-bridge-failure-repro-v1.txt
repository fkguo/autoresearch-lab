# M4.3 skill bridge failure repro (RED -> GREEN)

## RED (recorded before URI decode fix)

Command:
`pytest -q tests/hepar/test_skill_bridge_m43.py -k space_in_artifact_path_round_trips_uri`

Observed failure:
- `KeyError: 'rpc_result'`
- stack frame: `src/idea_core/hepar/skill_bridge.py` replay path
- root cause: replay used `Path(response_ref[7:])` and treated `%20` in `file://` URI as literal chars, so cached response file was not found.

Trace excerpt:
```
E   KeyError: 'rpc_result'
.../skill_bridge.py:109 in execute
    "rpc_result": response_payload["rpc_result"],
```

## GREEN (after fix)

Command:
`pytest -q tests/hepar/test_skill_bridge_m43.py`

Observed result:
```
.....                                                                    [100%]
```

Fix summary:
- Added URI-safe file path decoding (`urlparse` + `unquote`) in `HeparSkillBridge._artifact_ref_to_path`.
- Replay now resolves `file://` artifact refs robustly, including paths containing spaces.
- Added regression test: `test_replay_with_space_in_artifact_path_round_trips_uri`.
