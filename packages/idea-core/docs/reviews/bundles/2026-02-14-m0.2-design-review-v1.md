# Review Bundle: 2026-02-14 M0.2 Design (v1)

## 1. Scope in this review

- Milestone: **M0.2 only**
- Goal: validate the **layered design** (idea-core control-plane + Julia compute-plane), the **I/O contract**, and the **gate/sync rules** for the “Pion GFF Bootstrap” M5 test project.
- Explicitly in scope:
  - control vs compute split (responsibilities, directory layout)
  - compute I/O contract (`config.json` → `results.json`)
  - “modern bootstrap” insertion point (avoid pure dispersive reproduction)
  - evidence-first + dual-review + tracker/GitHub sync requirements
- Explicitly out of scope:
  - any actual Julia implementation or numerical results (M0.3+)
  - coupled-channel methods (disallowed by hard constraints)

## 2. Files under review

- `docs/research/pion-gff-bootstrap/m0.2-design.md`

## 3. Acceptance mapping (M0.2)

1) **Layered architecture is explicit** and avoids idea-core becoming a physics compute library.

2) **Compute-plane I/O contract is concrete**:
   - required input fields are specified (`config.json`)
   - required outputs are specified (`results.json` + required keys)

3) **Modern bootstrap is the driver**:
   - design states what is bootstrapped (e.g., elastic partial waves / phase shifts family)
   - design explicitly rejects “pure dispersive reproduction” as the main method

4) **Evidence-first gates and sync are specified**:
   - milestone gate artifacts listed for M0.2
   - dual-review policy included (Opus + gemini-3-pro-preview)
   - tracker + GitHub Project sync are treated as required evidence

5) **“Don’t forget” requirement is internalized**:
   - design states intent to encode gates as machine-checkable artifacts (not only prose)
   - design provides a clear plan to enforce this in M0.3

## 4. Evidence pointers

- Project tracker (append-only): `docs/research/pion-gff-bootstrap/tracker.md`
- Preflight (M0.1): `docs/research/pion-gff-bootstrap/m0.1-preflight.md`
- Design (M0.2): `docs/research/pion-gff-bootstrap/m0.2-design.md`

