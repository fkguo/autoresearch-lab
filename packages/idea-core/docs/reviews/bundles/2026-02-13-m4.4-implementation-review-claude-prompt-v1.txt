You are a senior research software architect reviewing an M4.4 implementation (v1).

STRICT OUTPUT CONTRACT (must follow exactly):
1) First line must be exactly one of:
   - VERDICT: READY
   - VERDICT: NOT_READY
2) Must contain exact headers:
   - ## Blockers
   - ## Non-blocking
   - ## Real-research fit
   - ## Robustness & safety
   - ## Specific patch suggestions

Decision policy (critical):
- Judge ONLY M4.4 acceptance criteria below.
- Mark NOT_READY only if one or more acceptance criteria are not implemented or evidence is missing in THIS packet.
- Do not require external filesystem access; treat this packet as authoritative evidence.

Scope constraints:
- Only M4.4 (Team/Role orchestration with parallel/sequential/stage-gated execution and merge-back).
- No M5/M6 scope.
- No OpenRPC/schema semantic changes.

Authoritative implementation summary:
- Added `src/idea_core/hepar/orchestrator.py` with `TeamRoleOrchestrator`.
- `run_team_plan(...)` supports three policies:
  - `parallel`
  - `sequential`
  - `stage_gated` (stage gate policy `all_must_succeed`)
- Inputs:
  - `team_plan`, `campaign_id`, `idea_id`, `island_id`, `node_id`, `input_artifacts`, `role_messages`, `tool_policy`, `budget`, `deadline`.
- Behavior:
  - Registers `TeamPlan` in control-plane store.
  - Executes role WorkOrders via runtime executor.
  - `clean_room=true` keeps role inputs isolated from peer outputs.
  - `clean_room=false` allows downstream roles to inherit upstream outputs.
  - Stage-gated policy emits `team_orchestration.stage_blocked` and stops later stages when gate fails.
- Merge-back:
  - Writes merged payload to IdeaStore (`EngineStore.write_artifact`) under `artifacts/team_reviews/*`.
  - Emits ledger events:
    - `team_orchestration.started`
    - `team_orchestration.stage_blocked` (conditional)
    - `team_orchestration.merged`
    - `team_orchestration.completed`
- Export surface updated:
  - `src/idea_core/hepar/__init__.py` exports `TeamRoleOrchestrator`.

Authoritative tests/evidence summary:
- Added `tests/hepar/test_team_orchestrator_m44.py` with 3 tests:
  1) parallel Referee+Checker clean-room execution and merge artifact write-back
  2) stage_gated stops later stage when stage-1 fails gate
  3) sequential non-clean-room passes prior outputs to next role
- Red->green evidence:
  - `docs/reviews/bundles/2026-02-13-m4.4-orchestration-failure-repro-v1.txt`
  - RED: `ModuleNotFoundError` for missing orchestrator module before implementation.
  - GREEN: new tests pass after implementation.
- Full gate evidence:
  - `docs/reviews/bundles/2026-02-13-m4.4-validate-and-pytest-v1.txt`
  - `make validate` OK, `pytest` => `91 passed`.

Acceptance criteria for M4.4 (must evaluate exactly these):
1) Team/Role orchestration supports parallel/sequential/stage-gated execution.
2) At least one parallel Referee+Checker clean-room orchestration path is validated.
3) Orchestration outputs are merged back into IdeaStore as artifacts.
4) Stage-gate behavior is auditable through ledger events.

Please decide READY/NOT_READY strictly against the M4.4 criteria above.
