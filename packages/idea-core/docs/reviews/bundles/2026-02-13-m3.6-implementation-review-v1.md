# Review Bundle: 2026-02-13 M3.6 minimal_compute_plan resource calibration (v1)

## 1. Scope in this review

- Milestone: **M3.6 only**
- Goal: calibrate `minimal_compute_plan` resource estimation for HEP realism, covering default rubric + anti-pseudo-computable gate.
- Explicitly out of scope: M4+/other milestones, OpenRPC/schema semantic edits.

## 2. Files under review

- `src/idea_core/engine/service.py`
- `tests/engine/test_compute_plan_rubric_m36.py`
- `docs/reviews/bundles/2026-02-13-m3.6-compute-rubric-failure-repro-v1.txt`
- `docs/reviews/bundles/2026-02-13-m3.6-validate-and-pytest-v1.txt`

## 3. Acceptance mapping (M3.6)

1. 默认 HEP rubric 覆盖 `estimated_compute_hours_log10` + `required_infrastructure`
   - Introduced deterministic rubric rules (`toy_laptop`, `batch_workstation`, `heavy_cluster`, `frontier_not_yet_feasible`).
   - `eval.run` feasibility diagnostics now backfill missing step fields:
     - `minimal_compute_plan[i].required_infrastructure`
     - `minimal_compute_plan[i].estimated_compute_hours_log10`

2. 避免“看似可算但实际不可算”
   - Added structured feasibility findings:
     - `required_infrastructure_below_rubric`
     - `estimated_compute_hours_below_rubric`
   - Both map to `failure_mode=not_computable`; heavy-method underestimation escalates to `critical` and is promotion-blocking.

3. 与 `eval/promote` 路径可回归
   - `eval.run` emits deterministic `hep:feasibility:...` tokens for rubric underestimation.
   - `node.promote` existing critical-HEP gate blocks affected nodes.
   - Regression tests cover:
     - default field calibration path (missing fields -> rubric defaults)
     - underestimated heavy compute path (`eval.run` emission + `node.promote` blocking)

## 4. TDD red-green evidence

- `docs/reviews/bundles/2026-02-13-m3.6-compute-rubric-failure-repro-v1.txt`
  - RED: new tests fail before implementation
    - missing defaults (`required_infrastructure` KeyError)
    - missing rubric-underestimation failure mode
  - GREEN: tests pass after implementation

## 5. Full gate evidence

- `docs/reviews/bundles/2026-02-13-m3.6-validate-and-pytest-v1.txt`
  - `make validate` OK
  - `pytest` all green (`67 passed`)

## 6. Source excerpts (clean-room)

### `src/idea_core/engine/service.py` (rubric + defaults)
```python
HEP_COMPUTE_RUBRIC_RULES = (...)

def _infer_hep_compute_rubric(...):
    ...

if not raw_required_infra:
    step["required_infrastructure"] = rubric_infra
...
if not cls._is_number(estimate_value):
    step["estimated_compute_hours_log10"] = rubric_estimate
```

### `src/idea_core/engine/service.py` (anti-pseudo-computable checks)
```python
if cls._infrastructure_rank(required_infra) < cls._infrastructure_rank(rubric_infra):
    add_finding(code="required_infrastructure_below_rubric", severity="critical" ...)

if rubric_estimate - estimate >= 0.7:
    add_finding(code="estimated_compute_hours_below_rubric", severity="critical" ...)
```

### `tests/engine/test_compute_plan_rubric_m36.py`
```python
def test_eval_run_applies_hep_default_rubric_for_missing_compute_fields(...):
    ...
    assert step["required_infrastructure"] == "cluster"
    assert step["estimated_compute_hours_log10"] >= 3.0


def test_node_promote_blocks_underestimated_heavy_compute_plan(...):
    ...
    assert any("required_infrastructure_below_rubric" in mode for mode in failure_modes)
    assert any("estimated_compute_hours_below_rubric" in mode for mode in failure_modes)
```

## 7. Reviewer checklist

- [ ] HEP 默认 rubric 已实现且可回填缺省 compute fields。
- [ ] 能识别并阻断“低估算力需求”的伪可算方案。
- [ ] `eval.run -> node.promote` 回归链路完整。
- [ ] 无 OpenRPC/schema 语义漂移。
- [ ] 无 M4+/跨阶段 scope creep。
