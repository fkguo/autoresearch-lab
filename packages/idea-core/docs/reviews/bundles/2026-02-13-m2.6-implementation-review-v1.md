# M2.6 Implementation Review Bundle (v1)

Date: 2026-02-13  
Repo: `/Users/fkg/Nutstore Files/Coding/Agents/idea-core`  
Milestone: `M2.6` (`search.step` 最小 operator 闭环)

## 1) 目标 / 范围 / 不做什么

### 目标
- 在 `search.step` 落地最小 operator 框架。
- 接入至少 2 个 deterministic dummy operators。
- 让 `search.step` 可生成并落盘 schema-valid 新节点，并输出可审计轨迹。
- 保持幂等 replay/conflict 语义和预算语义。
- 验证新节点可进入最小下游闭环：`eval.run -> rank.compute -> node.promote`。

### 实施范围
- `src/idea_core/engine/operators.py`
- `src/idea_core/engine/service.py`
- `tests/engine/test_search_step_operator_m26.py`
- `tests/engine/test_search_step_island_state_machine.py`

### 不做什么（明确排除）
- 不修改 `contracts/idea-generator-snapshot` 内任何 schema/OpenRPC。
- 不实现 `M2.7`（Explain-Then-Formalize 强制）及 `M3+` 领域扩展能力。
- 不引入外部检索/arXiv/网络抓取能力。

## 2) 代码差异（Diff 摘要）

Commits:
- `4635fcc` `feat(engine): add deterministic dummy search operator interface`
- `216cba7` `feat(search): wire deterministic operators into search.step node creation`
- `847df7f` `test(search): cover M2.6 operator loop and idempotent closed flow`

Changed files:
- `src/idea_core/engine/operators.py`
- `src/idea_core/engine/service.py`
- `tests/engine/test_search_step_island_state_machine.py`
- `tests/engine/test_search_step_operator_m26.py`

Key behavior changes:
- 新增最小 operator 接口与 2 个 dummy operator（`dummy.expand.bridge` / `dummy.constraint.shift`）。
- `search.step` 引入 deterministic round-robin operator 选择，并每 tick 生成 1 个新节点写入 IdeaStore。
- 新节点完整满足 `idea_node_v1`（含 `origin/operator_trace/rationale_draft/idea_card` 等字段）。
- `search.step` 返回 `new_node_ids` 与 `new_nodes_artifact_ref`，并与实际落盘一致。
- 新增 per-tick operator trace artifacts（`operator_id/backend_id/island_id` + artifact refs）。
- `search.step` 的 idempotency prepared-commit 检查增强：新增节点场景要求 `new_nodes_artifact_ref` 与节点落盘一致。
- 预算计数更新：`search.step` 在 M2.6 最小实现下同时计 `steps_used` 和 `nodes_used`。

## 3) 命令与输出（验收证据）

```bash
make validate && pytest
```

Output:

```text
python3 -m idea_core.contracts.validate
[validate] OK: JSON parse, schema legality, OpenRPC minimal shape, $ref closure, drift-guard
..................................                                       [100%]
34 passed in 0.63s
```

## 4) 契约对齐清单（Contract Alignment Checklist）

- [x] 未改动 schema/OpenRPC SSOT 快照内容。
- [x] `search.step` 返回仍符合 `search_step_result_v1.schema.json`。
- [x] 当 `new_node_ids` 非空时，返回 `new_nodes_artifact_ref`（满足条件 required）。
- [x] 新增节点逐个通过 `idea_node_v1.schema.json` 校验后才提交。
- [x] `node.list`/`node.get` 对 `search.step` 新节点可见。
- [x] 幂等 replay：同 key 同 payload 返回同结果且 `is_replay=true`，不重复副作用。
- [x] 幂等冲突：同 key 不同 payload 返回冲突错误，包含 `payload_hash`。
- [x] 下游最小联动可用：`search.step` 新节点可被 `eval.run`、`rank.compute`、`node.promote` 消费。

