You are a senior research software architect reviewing an M2.7 implementation.

STRICT OUTPUT CONTRACT (must follow exactly):
1) First line must be exactly one of:
   - VERDICT: READY
   - VERDICT: NOT_READY
2) Must contain exact headers:
   - ## Blockers
   - ## Non-blocking
   - ## Real-research fit
   - ## Robustness & safety
   - ## Specific patch suggestions

Task:
Review whether M2.7 is complete and acceptable.

Scope constraints:
- Only M2.7 (Explain-Then-Formalize enforcement: RationaleDraft -> IdeaCard).
- No M2.8+/M3+ expansion.
- No contract drift (OpenRPC/schema snapshot unchanged).

Implemented artifacts:
- src/idea_core/engine/service.py
- tests/engine/test_node_promote.py
- tests/engine/test_search_step_operator_m26.py
- Review bundle: docs/reviews/bundles/2026-02-13-m2.7-implementation-review-v1.md

Expected behavior implemented:
1) Seed/operator node creation uses a single formalize helper from rationale_draft to idea_card.
2) operator_trace.params includes formalization audit fields (mode/source/rationale_hash/input summary).
3) node.promote fails when idea_card is missing using existing schema error model.
4) promote idempotency replay does not duplicate side effects.
5) same idempotency key + different payload fails with payload_hash present.

Verification evidence:
- make validate && pytest -q -> 41 passed, validate OK.
- New tests:
  - test_node_promote_fails_when_idea_card_missing
  - test_node_promote_idempotency_replay_does_not_duplicate_side_effects
  - test_node_promote_idempotency_conflict_includes_payload_hash
  - test_generated_nodes_visible_via_node_list_cursor_and_node_get (formalization trace asserts)

Please decide READY/NOT_READY with concrete blockers only if they prevent accepting M2.7.
